<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- PERMISSIONS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="true" />

    <application
        android:name=".LocationSourceApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocationSource"
        tools:targetApi="31">

        <!-- SPLASH SCREEN -->
        <activity
            android:name=".ui.activity.splash.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LOCATION SOURCE SCREEN -->
        <activity
            android:name=".ui.activity.home.LocationSourceScreenActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"/>

        <!-- ADD LOCATION SOURCE SCREEN -->
        <activity
            android:name=".ui.activity.place.AddPlaceScreenActivity"
            android:screenOrientation="portrait"
            android:exported="false"
            android:parentActivityName=".ui.activity.home.LocationSourceScreenActivity"
            android:windowSoftInputMode="adjustPan"/>

        <!-- LOCATION SOURCE ROUTE DETAILS SCREEN -->
        <activity
            android:name=".ui.activity.place.RouteDetailsScreenActivity"
            android:screenOrientation="portrait"
            android:exported="false"
            android:parentActivityName=".ui.activity.home.LocationSourceScreenActivity"
            android:windowSoftInputMode="adjustResize"/>


        <!--GOOGLE MAP PLACES KEY-->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_map_key" />

    </application>

</manifest>